-- Create "blob_chunks" table
CREATE TABLE "blob_chunks" ("id" bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY, "upload_id" character varying NOT NULL, "session_id" character varying NOT NULL, "range_from" bigint NOT NULL, "range_to" bigint NOT NULL, PRIMARY KEY ("id"));
-- Create index "blobchunk_session_id" to table: "blob_chunks"
CREATE UNIQUE INDEX "blobchunk_session_id" ON "blob_chunks" ("session_id");
-- Create index "blobchunk_upload_id" to table: "blob_chunks"
CREATE UNIQUE INDEX "blobchunk_upload_id" ON "blob_chunks" ("upload_id");
